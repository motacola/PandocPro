#!/usr/bin/env bash
# Wrapper for the shared Byterover knowledge helper (store mode).
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
node "$SCRIPT_DIR/scripts/byterover-knowledge.js" store "$@"
