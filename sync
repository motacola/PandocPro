#!/usr/bin/env bash
# Quick launcher - resolves project directory dynamically and opens the menu

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$SCRIPT_DIR"

cd "$PROJECT_ROOT"
./scripts/menu.sh
